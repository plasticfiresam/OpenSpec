## Purpose

[Только для новой возможности: кратко опишите её назначение не менее чем
50 символами. Для изменения существующей возможности удалите этот раздел.]

## Capability Coverage

[Условный блок для клиентского приложения (мобильного, десктопного или
веб-интерфейса): удалите блок целиком, если change не про клиент. Заполните
каждую строку требованиями, которые закрывают аспект, либо `N/A` с краткой
причиной. Строки не зависят от бэкенда: строка про realtime, push и sync
относится к любому бэкенду, который может менять данные за спиной приложения.
Таблица не переносится в основную спецификацию при archive, поэтому каждое
правило, на которое она ссылается, должно жить в Requirement ниже.]

| Аспект | Requirement(s) или N/A | Что требует подтверждения человеком |
|---|---|---|
| Чтение данных: API и локальные источники, фильтры, сортировка, агрегация, вычисляемые значения, форматирование | | |
| UI-семантика: видимость и доступность элементов, специфичные для фичи, или исключение из политики проекта | | |
| Чистая бизнес-логика: таблицы решений для ветвлений и расчётов | | |
| Бизнес-операции сверх обычного CRUD через API | | |
| Внешние взаимодействия и API: видимое поведение при timeout, offline и pending | | |
| Состояние домена и приложения: состояния экрана loading, empty, error, partial и stale | | |
| Realtime, push и sync: инвалидация, перезапрос и видимое обновление активного экрана | | |
| Небольшой набор ключевых навигационных и сквозных пользовательских путей | | |
| Прочие затронутые аспекты: права, персистентность, deep links, фоновая работа, доступность, локализация, аналитика, приватность, выкатка | | |

## ADDED Requirements

[Перед сохранением замените все подсказки в квадратных скобках и удалите
неприменимый текст.]

[Для существующего требования используйте `MODIFIED Requirements` и перенесите
его полную актуальную версию со всеми Scenario. Для удаления или переименования
используйте штатные секции OpenSpec.]

[Опишите наблюдаемое поведение атомарными Requirements: один Requirement — одно
независимо проверяемое правило, а не весь экран или раздел системы. Для каждого
Requirement обязателен хотя бы один Scenario.

Имя каждого Scenario — это имя теста, который его докажет: по-английски, в
форме `<what happens> when <condition>`, без слов should и test. `design.md` и
`tasks.md` используют имя Scenario дословно как имя unit-, bloc_test- или
golden-теста; тело сценария (GIVEN/WHEN/THEN) пишется на языке спецификации.
У каждого наблюдаемого состояния экрана (loading, empty, error, partial, stale
и каждый загруженный вариант) свой Scenario, чтобы golden-сценарии
соответствовали Scenario один к одному. Пример:
`#### Scenario: keeps the last known balance visible when the refresh runs offline`.

Если проект определяет формат идентификаторов Requirements:

- используйте один уникальный и постоянный ID для каждого Requirement;
- сохраняйте ID при `MODIFIED`, не перенумеровывайте и не используйте повторно;
- не кодируйте в ID временный статус документа или форму описания требования;
- сохраняйте идентификаторы источников отдельно, как сведения о происхождении;
- если соглашения в проекте нет, не изобретайте новый namespace.

Дополнительно используйте только подходящие по смыслу формы:

- формулу или инвариант;
- классификатор, матрицу соответствий или разрешений;
- таблицу решений;
- таблицу или Mermaid-диаграмму состояний и переходов;
- контракт операции;
- правила отображения данных;
- наблюдаемое поведение при внешнем сбое;
- наблюдаемое обновление интерфейса.

Это не обязательные разделы и не отдельные типы Requirements. Не добавляйте
пустые или неприменимые формы и не придумывайте номинальные правила ради
покрытия списка.

Таблицу решений используйте для алгоритма на правилах, когда условия, правила и
действия в таблице яснее формулы, инварианта или другой нотации. Чистая
реализация правила сама по себе не делает его decision table.

Помечайте дополнительные формы жирной подписью, например `**Инвариант**`,
`**Матрица разрешений**`, `**Таблица решений**` или `**Переходы состояний**`:
заголовок `####` зарезервирован OpenSpec только для Scenario. Техническая
реализация описывается в `design.md`.]

### Requirement: [Стабильный ID — Название, если проект использует идентификаторы; иначе название]

[Сформулируйте нормативное правило с `SHALL` или `MUST`. Если правило дополнено
таблицей, контрактом или диаграммой, они являются нормативной частью Requirement.
Scenario приводит конкретный проверяемый пример и не повторяет механически все
строки таблицы.]

[Если структурированная нотация делает правило яснее, замените следующий
необязательный блок подходящей формой и при необходимости повторите его. Если
достаточно нормативного текста, удалите блок целиком.]

**[Название применимой формы]**

[Например: Формула, Инвариант, Матрица разрешений, Таблица решений, Переходы
состояний, Контракт операции или Правила отображения. Опишите нормативное
содержимое выбранной формы.]

#### Scenario: [what happens] when [condition]

- **GIVEN** [исходное состояние, если оно существенно]
- **WHEN** [условие или действие]
- **THEN** [наблюдаемый результат]
- **AND** [дополнительная гарантия, необязательно]
